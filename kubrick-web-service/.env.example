# ===========================================
# REQUIRED Configuration
# ===========================================

# Server (required)
NODE_ENV=development
PORT=3001
FRONTEND_URL=http://localhost:5173

# MongoDB (required)
MONGODB_URI=mongodb://localhost:27017/kubrick

# Storage Provider (required) - 'gcp' or 's3'
STORAGE_PROVIDER=gcp

# ===========================================
# GCP Cloud Storage (required if STORAGE_PROVIDER=gcp)
# ===========================================
GCP_PROJECT_ID=your-project-id
GCP_BUCKET_NAME=kubrick-videos
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json  # Optional - uses default credentials if not set

# ===========================================
# AWS S3 (required if STORAGE_PROVIDER=s3)
# ===========================================
# AWS_REGION=us-east-1
# AWS_BUCKET_NAME=kubrick-videos
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# ===========================================
# Optional Configuration
# ===========================================

# Logging - debug, http, info, warn, error (default: info)
LOG_LEVEL=debug

# Auth (future)
OKTA_ENABLED=false
# OKTA_ISSUER=https://your-org.okta.com/oauth2/default
# OKTA_CLIENT_ID=
# OKTA_AUDIENCE=api://default
